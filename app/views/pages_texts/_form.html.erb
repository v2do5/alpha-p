<%= form_for(@pages_text) do |f| %>
  <% if @pages_text.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pages_text.errors.count, "error") %> prohibited this pages_text from being saved:</h2>

      <ul>
      <% @pages_text.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :page_name %><br />
    <%= f.text_area :page_name %>
  </div>
  <div class="field">
    <%= f.label :page_text %><br />
    <%= f.text_area :page_text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
